package utils;

import Jama.Matrix;

/**
 * Runs Appriori All Algorithm to find the patterns in videos.
 * 
 * @author siavash
 *
 */
public class ApprioriAll {
    private Matrix data;

    /**
     * Creates an instance and runs the algorithm.
     * 
     * @param data
     *            Matrix which rows are events of the data
     * @param minSupport
     *            for item sets.
     */
    public ApprioriAll(Matrix data, double minSupport) {
        
    }

}
